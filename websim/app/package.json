{
  "name": "@websim/app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "UI plane (Vite root, added in WP11): presets, sliders, map, charts, badge/provenance, permalinks.",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "comlink": "^4.4.2"
  }
}
