# ---- Build output ----
Geography/bin/
Geography/build/
# Simulation results directory (anchored to repo root and Geography/, so it
# does NOT match the source package src/geography/output/).
/output/
/Geography/output/

# ---- Gradle machine-local state ----
.gradle/
Geography/.gradle/
Geography/gradle.properties

# ---- Logs & crash dumps (kept on disk as forensic artifacts, never tracked) ----
*.log
*.log.*
hs_err_pid*.log

# ---- OS / editor noise ----
Thumbs.db
desktop.ini
.DS_Store

# ---- Machine-generated classpath artifacts ----
scripts/cp.args

# ---- Redundant archive: the extracted, tracked copy lives at Geography/data/Streets.* ----
/Streets.zip
scripts/__pycache__/

# ---- Overleaf submission staging (regenerable) ----
# The canonical manuscript lives in docs/chapter/. This folder is assembled for
# upload only; it was once the sole home of the current chapter, which is why the
# v1.0 cleanup moved the .tex and .bib into docs/chapter/ before ignoring it.
/overleaf 2/
